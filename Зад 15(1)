#include <iostream>
using namespace std;
void PowerA3(double A, double& B)
{
    B = A * A * A;
}
int main(void)
{
    double arr[5] = { 1, 2, 3, 4, 5 };
    for (int i = 0; i < 5; i++)
    {
        PowerA3(arr[i], arr[i]);
        cout << arr[i] << ' ';
    }
    system("pause");
    return 0;
}
