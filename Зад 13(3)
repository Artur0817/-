#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    setlocale(LC_ALL, "Russian");
    int n, a, b;
    cout << "n=";
    cin >> n;
    a = 0;
    for (b = 1; b <= (2 * n - 1); b+=2)
    {
        a += b;
        cout << "результат" << a << endl;
    }
    return 0;
}
