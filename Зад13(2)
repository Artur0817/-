#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    setlocale(LC_ALL, "Russian");
    float a = 1, b = 1.1;
    int n, x;
    cout << "n=";
    cin >> n;
    for (x = 1; x <= n; x++)
    {
        a *= b;
        b += 0.1;
    }
    cout << "Ответ:" << a;
    return 0;

}
