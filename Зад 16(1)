#include<iostream>
#include<math.h>
using namespace std;
int main()
{
	setlocale(LC_ALL, "RUS");
	int N;
	cout << "Введите N (N>0)" << endl;
	cin >> N;
	int* arr = new int[N];
	for (int i = 0; i <= N; i++) 
  {
		arr[i] = i * 2 + 1;
		cout << arr[i]<<" ";
	}
	return 0;
}
