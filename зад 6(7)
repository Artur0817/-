#include <iostream>
#include <math.h>
using namespace std;
int a, x, z;
int main() 
{
	setlocale(LC_ALL, "Russian");
	cout << "a=";
	cin >> a;
	x = a;
	z = a;
	x = x * a; // во 2
	x = x * x; // в 4
	x = x * x; // в 8
	x = x * x; // в 16
	x = x / z; // в 15
	cout << "a^15 = " << x;
}
